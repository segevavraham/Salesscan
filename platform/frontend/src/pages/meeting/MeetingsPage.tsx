import { Container, Typography } from '@mui/material';

export default function MeetingsPage() {
  return (
    <Container>
      <Typography variant="h4">Meetings</Typography>
      <Typography>List of all meetings will appear here</Typography>
    </Container>
  );
}
